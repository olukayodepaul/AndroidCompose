<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.androidcompose">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidCompose"
        tools:targetApi="31">
        <activity
            android:name=".merrybelt.ui.home.home_ui.HomeActivity"
            android:exported="false"
            android:label="@string/title_activity_transfer"
            android:theme="@style/Theme.AndroidCompose" />

        <activity android:name=".merrybelt.ui.login.login_ui.MainActivity" />

        <activity
            android:name=".merrybelt.ui.transfer.transfer_ui.TransferActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AndroidCompose">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>